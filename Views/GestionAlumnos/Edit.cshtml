@model kisko.Models.StudentDTO;
@{
    ViewData["Title"] = "Edit";
}

<section class="container_gestion_proyecto">
    <div>
        <form class="proyecto_form" asp-action="Edit" method="POST">

            <div class="form-group">
                <label for="name">Nombre</label>
                <input class="form-control" type="text" name="name" id="name" maxlength="100" required asp-for="Name" value="@Model.Name">
            </div>

            <div class="form-group">
                <label for="lastname">Apellido</label>
                <input class="form-control" type="text" name="lastname" id="lastname" maxlength="100" required asp-for="Lastname" value="@Model.Lastname">
            </div>

            <div class="form-group">
                <label for="email">Correo</label>
                <input class="form-control" type="email" name="email" id="email" maxlength="100" required asp-for="Email" value="@Model.Email">
            </div>

            <button type="submit" class="btn btn-success">Guardar</button>
        </form>
    </div>
</section>

